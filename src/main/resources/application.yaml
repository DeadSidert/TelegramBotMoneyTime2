bot:
  adminId: 213411446
  token: 1392424435:AAFWlQHWheZY26S6xCMNXtRyN0E_7-8V6h8
  name: MoneyBot
  botUrl: https://t.me/Monetary24bot

server:
  port: 8082

spring:
  liquibase:
    enabled: true
    contexts: test
  jpa:
    properties:
      hibernate.show_sql: false
      hibernate:format_sql: false
    open-in-view: false
    hibernate.ddl-auto: none
  datasource:
    #  Note that the hostname, port and database name on url property and username and password properties will be ignored
    #  and it will be used default container values
    url: jdbc:postgresql://localhost:5437/postgres?user=postgres&password=postgres&currentSchema=public
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
    hikari:
      schema: public